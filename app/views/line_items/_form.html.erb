<%= form_with(model: line_item) do |form| %>
  <% if line_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(line_item.errors.count, "error") %> prohibited this line_item from being saved:</h2>

      <ul>
        <% line_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :armor_id, style: "display: block" %>
    <%= form.text_field :armor_id %>
  </div>

  <div>
    <%= form.label :equip_id, style: "display: block" %>
    <%= form.text_field :equip_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
